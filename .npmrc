# Strict, reproducible installs.
engine-strict=true
save-exact=true
resolution-mode=highest

# Keep the dependency graph honest: no phantom dependencies.
# Every package must declare what it imports.
shamefully-hoist=false
strict-peer-dependencies=false

# Reduce install payload on slow connections.
prefer-offline=true
